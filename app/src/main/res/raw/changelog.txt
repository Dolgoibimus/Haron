# v1.5 — In development
- "About" screen: app icon, version, features list, changelog, logs, GitHub, privacy policy, copyright
- "What's new" screen: expandable version history
- Power save mode toggle in Settings — disables all animations
- Battery threshold slider (0–50%) — auto-disable animations below set level (default 15%)
- ContentObserver: register on foreground only (saves battery in background)
- Swipe back from left edge on all screens: settings, terminal, library, storage, duplicates, tags, about, features, themes, animations
- Tag screens redesigned: separate management and create/edit screens with back navigation
- Code audit: logging gaps filled, dead code removed, KDoc comments on 36 classes, architecture docs
- Fix: audio player played wrong track when opened via file name tap
- Fix: audio player buttons (play/prev/next) unresponsive in full screen
- Fix: audio no autoplay on open, always starts from beginning
- Fix: progress bar hidden behind custom navbar

# v1.4 — Release 2
- 4 overlay animations: Matrix Rain, Snowfall, Starfield, Dust Particles with individual settings
- Custom navbar replaces system navigation bar with configurable pages and buttons
- Radial menu: long press for Copy/Move, Delete/Force Delete, Create File/Folder
- Arrow key navigation with cursor highlight, Shift mode for multi-selection
- Chromecast / DLNA / Miracast casting with HLS transcoding (FFmpeg)
- Screen mirroring via MediaProjection, photo slideshow and PDF presentation on TV
- Yandex Disk / Google Drive / Dropbox: chunked upload with resume
- FTP/SFTP/SMB/WebDAV media streaming without download
- Full terminal: VT100/xterm emulation, ANSI colors, SSH via JSch, tabs
- Wi-Fi Direct and Bluetooth OPP file transfer
- FTP client + FTP server, SFTP client, SMB browser, WebDAV browser
- Protected folder with AES-256 encryption (Android Keystore)
- Steganography: hide files inside PNG images
- Trusted devices with auto-accept transfers
- Audio tag editing with album cover search (Deezer + iTunes)
- PDF and FB2 metadata in file properties
- Checksums: CRC32, MD5, SHA-1, SHA-256, SHA-512 with hash comparison
- Archive "1 to 1": each file into separate ZIP
- Thumbnails for images inside archives
- DND mode in video player
- File duplication with 3 destination modes
- Per-file progress for folder operations with speed display
- Fix: parallel uploads killed connections — sequential via Mutex
- Fix: Yandex MIME throttling — upload as .tmp + rename
- Fix: Dropbox offset correction on resume
- Fix: AVI artifacts in VLC — software decoder for non-standard codecs
- Fix: cast reconnect after transcode timeout
- Fix: HLS version 3 and AAC stereo for Chromecast compatibility
- Fix: navbar page swipe conflict with radial gesture

# v1.0 — Release 1
- Dual-panel file explorer with drag-and-drop
- Copy, move, delete, rename with conflict resolution (replace/rename/skip)
- Trash (recycle bin) with 30-day auto-cleanup
- Archive support: ZIP, 7z, RAR, TAR, TAR.GZ with password protection
- Built-in text editor with syntax highlighting
- PDF reader with text search and page navigation
- Document viewer: EPUB, FB2, DOCX, ODT, XLSX
- Media player (VLC): audio + video with playlist
- Gallery with pinch-to-zoom and swipe navigation
- File properties: EXIF data with removal option
- Full-text search (FTS5) + content grep
- Voice commands (30+ commands)
- Gesture system (4 edge swipes)
- Storage analyzer with pie chart
- Duplicate detector (hash-based)
- Folder and text file comparison (side-by-side diff)
- App manager: installed apps, extract/install APK
- Bookmarks, tags, shelf (favorites panel)
- Shizuku support for Android/data access
- USB OTG storage
- Open files from other apps (ACTION_VIEW / ACTION_SEND)
