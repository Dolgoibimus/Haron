# Batch 103 — Power saving, About screen (v1.5)
- "About" screen in drawer: icon, version, features, changelog, logs, GitHub, privacy, copyright
- "What's new" screen with expandable version history
- Power save mode: disables animations, reduces background activity
- Battery threshold slider (0–50%): auto-disable animations below set level
- ContentObserver: foreground-only registration (saves battery)
- Fix: progress bar hidden behind custom navbar

# Batch 102 — Code audit
- Logging added to 9 files, dead code removed (invokeFromUri)
- android.util.Log replaced with EcosystemLogger in TerminalBuffer
- KDoc comments on 36 key classes
- Architecture dependency tree in AGENTS.md

# Batch 101 — Swipe-back, audio player fixes
- Swipe back from left edge on all settings/terminal/library/storage/duplicates/tags screens
- Tag screens redesigned: separate create/edit with back navigation
- Fix: audio played wrong track from file tap (setPlaylist missing for audio)
- Fix: audio buttons unresponsive in full screen (gesture overlay intercepted taps)
- Fix: audio no autoplay, always starts from beginning

# Batch 100 — Network streaming, DND (v1.4)
- FTP/SFTP/SMB/WebDAV media streaming without download
- HTTP proxy for FTP streams (VLC cyrillic workaround)
- DND mode in video player
- Saved FTP/SMB server list with credentials
- Fix: cast reconnect after transcode timeout

# Batch 90 — Animated themes
- 4 overlay animations: Matrix Rain, Snowfall, Starfield, Dust
- Individual settings: speed, density, opacity, size
- Matrix Rain: 5 charsets, custom RGB, glow effect
- "Only while charging" option, auto-pause in background

# Batch 89 — Custom navbar, radial menu
- Custom navbar replaces system nav bar
- Configurable pages with 5-7 buttons
- Radial menu: Copy/Move, Delete/Forever, File/Folder
- Custom icons for navbar buttons
- Arrow navigation with cursor, Shift multi-select
- Fix: navbar swipe conflict with radial gesture

# Batch 87 — Cloud uploads
- Yandex/GDrive/Dropbox chunked upload with resume
- Fix: parallel uploads killed connections (Mutex)
- Fix: Yandex MIME throttling (.tmp rename)
- Fix: Dropbox offset correction on resume

# Batch 83 — Foreground service file operations
- Copy/move/delete run in foreground service (unkillable)
- Per-file progress with speed display
- Cancel button stops immediately

# Batch 75 — File duplication, archive extraction
- Duplicate files: long tap Copy → count + 3 destinations
- Quick extract: select archives → pulsing icon → choose destination
- Archive "1 to 1": each file into separate ZIP

# Batch 68 — FTP server/client
- FTP server for receiving files
- FTP client with connection pooling

# Batch 54 — Cast, TV remote
- Chromecast/DLNA/Miracast with HLS transcoding (FFmpeg)
- Screen mirroring, photo slideshow, PDF presentation on TV
- Fix: HLS version 3 + AAC stereo for Chromecast

# Batch 53 — HLS transcoding
- FFmpeg progressive cast: play while transcoding
- Transcode cache with configurable TTL

# Batch 41 — Trusted devices, transfers
- Wi-Fi Direct and Bluetooth file transfer
- Trusted devices with auto-accept
- Device rename and aliases

# Batch 38 — Steganography, security
- Hide files inside PNG images (LSB encoding)
- Protected folder: AES-256 (Android Keystore)

# Batch 37 — Terminal
- Full VT100/xterm emulation with ANSI colors
- SSH via JSch, tabs (Shell/SSH)
- Tab completion, clickable paths

# Batch 36 — File/folder comparison
- Side-by-side text diff with color coding
- Recursive folder comparison with MD5 hashes

# Batch 22 — Full-text search (v1.0)
- FTS5 index + content grep
- ML Kit image labeling for search

# Batch 14 — Duplicate detector, shelf
- Hash-based duplicate detection
- Favorites shelf panel

# Batch 12 — Archives, gallery, PDF
- ZIP/7z/RAR/TAR support with passwords
- Built-in gallery with pinch-to-zoom
- PDF reader with search

# Batch 10 — Media player, text editor
- VLC audio+video with playlist
- Text editor with syntax highlighting

# Batch 4 — Drag-and-drop, trash
- Drag-and-drop between panels
- Trash with 30-day auto-cleanup

# Batch 1 — Core file manager (v1.0)
- Dual-panel explorer
- Copy, move, delete, rename
- Sort, filter, hidden files
