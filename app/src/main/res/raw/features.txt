# Navigation & Interface
- Two-panel vertical layout (top + bottom)
- Draggable panel divider with 3 tap zones (bookmarks / 50-50 / tools)
- Folder navigation with back button
- Clickable breadcrumbs for quick path navigation
- Navigation history per panel (← → like a browser)
- Open current folder in other panel
- Active panel highlighted with border
- Scroll position preserved per panel

# Files & Folders
- File list with type icons
- Preview thumbnails: images, video, text, PDF, documents, APK, FB2
- Scalable grid: 1–6 columns via pinch gesture
- Tap zones in list: tap → open, long tap left → select, long tap right → quick send, icon tap → preview
- Tap zones in grid: icon tap → preview, long tap → select, name tap → open, long tap name → quick send
- File extension badge on icons
- Empty folders visually highlighted
- Sort by name, date, size, extension
- Show/hide hidden files
- Quick search in current folder
- Create templates: folder, TXT, MD, dated folder

# Selection & Operations
- Long tap + swipe for range selection
- Select all / deselect all (long tap → select by extension)
- File counter and total size of selection
- Copy and move between panels
- Delete to trash
- Inline renaming (like Windows)
- Batch rename: template, numbering, find & replace
- Drag-and-Drop between panels (single and group)
- Copy or Move chooser: drag file across divider — left side copies, right side moves [NEW]
- Duplicate files: long tap Copy → dialog with number of copies and 3 destinations [NEW]
  - To subfolder here: creates folder named after file, copies inside [NEW]
  - To other panel (subfolder): same but in the other panel's folder [NEW]
  - To other panel (open folder): copies directly into the other panel's current folder [NEW]
  - Copy names: filename(1).ext, filename(2).ext, etc. [NEW]
  - Folder size updates in real time during duplication [NEW]
- Conflict dialog: replace / rename / skip

# Trash
- Deleted files stored for 30 days
- View, restore, clear trash
- Auto-cleanup when size limit exceeded
- Size limit: 0–5000 MB
- Per-file deletion progress (shows each file being deleted)
- Auto-recovery of records from filesystem if metadata is corrupted

# Background Operations
- Foreground Service for large operations (>10 files or >50 MB)
- Notification with progress and cancel button
- Progress bar at bottom of screen

# Side Menu (Drawer)
- Storages: internal + SD-cards
- Favorites: favorite folders
- Recent: last 5 visited folders
- Tools: trash, analyzer, duplicates, empty folders, force delete, tags, transfer, terminal
- Appearance: system / light / dark theme
- Settings

# Temporary Shelf
- File buffer for moving between distant folders
- "To Shelf" button when files selected
- Paste / Move from shelf
- Shelf persists between sessions

# Storage (SAF)
- SD-card and external storage support
- All file operations work with content:// URI
- Access to Android/data and Android/obb via Shizuku (Android 11+) [NEW]

# Quick Preview
- Tap file icon → preview dialog
- Images, video, audio, text, PDF, archives, APK, documents, FB2
- Play media in preview
- Open in gallery / reader / archive buttons
- Delete images directly from preview (trash button) — moves to next photo [NEW]

# Media Player
- Built-in VLC engine (LibVLC)
- All popular codecs: MP3, FLAC, OGG, WAV, AVI, MKV, MP4, etc.
- Folder playlist with auto-advance
- Background playback with lock screen controls
- Seekbar + ±10s rewind
- Position saved between sessions
- Video: fullscreen, immersive mode

# Internal Gallery
- Fullscreen image viewing
- Swipe between images in folder
- Zoom: pinch (1x–5x) and double tap
- Share button
- Pre-load adjacent images

# PDF Reader
- Fullscreen PDF reader
- Scroll, zoom (pinch + double tap)
- Page indicator with quick jump
- Search highlight with navigation
- Password-protected PDF support
- Position saved between sessions

# Document Reader
- DOC, DOCX, ODT, RTF, FB2, XLSX, XLS, CSV
- FB2: cover + annotation before text
- Zoom and scroll
- Search highlight with navigation
- Position saved between sessions

# Archives
- View ZIP, 7z, RAR (including RAR5), TAR, TAR.GZ/TGZ, TAR.BZ2/TBZ2, TAR.XZ/TXZ [NEW]
- Inline viewing in panel like regular folder
- Navigate folders inside archive
- Selective extraction
- Create ZIP with optional password (AES-256) and split
- Conflict dialog on create: replace / rename / cancel
- Extract options: extract here or into subfolder by archive name
- Smart detection: if archive has single root folder — extract as-is without dialog
- Quick extract from file list: select archives (ZIP/7z/RAR) — Archive icon pulses and changes to Extract [NEW]
- Tap pulsing icon → choose destination: next to archive / current panel / other panel [NEW]
- Each archive extracts into a subfolder with the archive name (auto-created) [NEW]
- Works with single or multiple archives at once [NEW]
- "1 to 1" archiving: select multiple files → Archive → "1 to 1" — each file becomes a separate ZIP [NEW]

# Text Editor
- View and edit TXT/MD files
- View mode (default) and edit mode
- Undo/Redo (up to 50 steps)
- Save dialog on exit with unsaved changes
- Cursor position saved between sessions
- Large files (600KB+) open in Sora Editor without freezes [NEW]

# File Properties
- Name, path, size, date, MIME-type, permissions
- Recursive folder size calculation
- EXIF data for photos (with delete option)
- Audio tags: title, artist, album, year, genre, duration, bitrate (editable) [NEW]
- Save/edit audio tags directly in file properties [NEW]
- Album cover search from internet (Deezer + iTunes) with save to file [NEW]
- Search cover by filename — auto-parse artist and title [NEW]
- Smart search: one search fills all missing tags (title, artist, album, year, genre) + cover [NEW]
- "Save all" button: save tags and cover together in one tap [NEW]
- PDF metadata: title, author, subject, keywords, creator, producer, creation date, page count [NEW]
- FB2 metadata: title, author, genre, language, series, date, annotation [NEW]
- Checksums: CRC32, MD5, SHA-1, SHA-256, SHA-512 [NEW]
- Hash comparison: paste expected hash — instant match/mismatch check [NEW]

# Storage Analyzer
- Pie chart by file type
- Categories with size and file count
- Top 200 largest files per category
- Large files (>10 MB) section

# Duplicate Detector
- Find identical files across storage
- Two-phase: group by size → MD5 hash
- Original markers: original is kept, copies are suggested for deletion
- Source folders mode: files in selected folders are automatically treated as originals
- Auto-select copies for deletion

# Settings
- Night mode by schedule
- Font size and icon size sliders
- Haptic vibration toggle
- Security: PIN, biometric, security question
- Require PIN on launch toggle
- Trash size limit
- Shizuku status and control (install, start, grant permission) [NEW]

# Tags (Labels)
- Colored labels with text names (8 colors)
- Assign to files and folders
- Filter by label
- Labels persist on rename/move/copy

# Desktop Widget
- Favorites list on home screen
- Tap folder → app opens there

# Quick Bookmarks (1–6)
- Tap left divider zone → pie menu
- Save/navigate to folders instantly
- Persistent between launches

# Tool Pie-Menu
- Tap right divider zone → pie menu
- Trash, Analyzer, Duplicates, APK, Player, Reader

# App Lock
- PIN (4–8 digits) with auto-enter
- Biometric (fingerprint)
- Combined: biometric + PIN backup
- Timeout lock (30 min)
- Security question for PIN recovery
- Screenshot protection when locked

# Protected Folder
- AES-256-GCM encryption (Android Keystore)
- Files completely invisible without shield
- Shield button requires PIN
- Long tap shield → enter protected files
- Tap shield in protected mode → exit
- Virtual directory for all protected files
- Copy decrypts, move decrypts + unprotects

# Global Search
- Long tap search → enter global search
- Search by name across entire device
- Filters: category, size, date
- Content search: text files, DOCX, ODT, PDF
- Audio/video metadata search
- ML Kit image labeling (objects on photos)
- OCR on photos (text recognition)
- Archive search (file names inside)
- Indexing: basic (text, metadata, archives), media (audio/video tags), visual (ML + OCR)
- Background index update on file changes and screen on (every 30 min)

# Local Search (in Panel)
- Name search: filters files in current folder by name
- Content search: finds text inside files (TXT, DOCX, ODT, PDF, etc.)
- First content search indexes the folder, subsequent searches are instant
- Debounce 300ms, auto-reset on folder change

# Barcode & QR Scanner
- Built-in scanner via camera (ML Kit)
- Scan QR codes, barcodes (EAN, UPC, Code128, etc.)
- How to open: long tap the scanner button in Transfer screen, or voice command "scanner" / "scan" / "qr"
- Scan result: open link, copy text, or search

# File Transfer
- Wi-Fi Direct + NSD (LAN)
- Auto-select best protocol
- QR code transfer — two modes (tap QR to switch): [NEW]
  Wi-Fi mode (both devices on the same network): [NEW]
  1. Select files → Transfer → "HTTP / QR code" [NEW]
  2. QR code with download link — scan on the other device [NEW]
  Hotspot mode (devices on different networks or no Wi-Fi): [NEW]
  - Tab "Haron": one QR code — second Haron scans it, auto-connects to hotspot and downloads [NEW]
  - Tab "Others": two QR codes for any device without Haron — first for Wi-Fi connection, second for download link in browser [NEW]
  - Hotspot is temporary, stops automatically when dialog is closed [NEW]
- Progress, resume, retry on disconnect
- Foreground Service + WakeLock

# Cast to TV
- Chromecast, Miracast, DLNA support
- Media, slideshow, PDF presentation, screen mirror (via browser) modes
- Remote control: play/pause, seek, volume
- Transcoding: AVI, MKV, WMV, MOV, FLV, 3GP, TS → MP4 for Chromecast
- Transcode caching — instant replay of previously transcoded files
- Automatic audio fallback for unsupported codecs (AC3/DTS)

# Bluetooth HID Remote [NEW]
- Touchpad mode: move cursor on TV, tap, long press, scroll [NEW]
- Virtual keyboard: type text on TV screen (Latin + Cyrillic) [NEW]
- Connect to paired Bluetooth devices [NEW]

# Network & SMB
- Auto-discover devices on LAN
- SMB browser with dual-panel mode
- Upload/download with progress
- Saved connections (encrypted)

# FTP Client & Server [NEW]
- Connect to remote FTP servers [NEW]
- Browse, download, upload files with progress [NEW]
- Dual-panel: FTP files on top, local files on bottom [NEW]
- FTPS (TLS) support [NEW]
- Saved FTP connections (encrypted credentials) [NEW]
- Built-in FTP server: share phone files to PC [NEW]
- Anonymous or password-protected access [NEW]
- Read-only mode option [NEW]

# Quick Send
- Long tap file → device circles → drag to send
- Trusted devices auto-accept
- Device renaming (aliases)

# USB OTG
- Auto-detect USB drives
- Navigate, safe eject
- Free/total space indicator

# Gestures
- 4 customizable edge swipes
- 13 available actions
- Configure in Settings

# Voice Commands
- 35+ commands in Russian and English
- Tap mic — single command, tap again — stop
- Long tap mic — list of all commands
- Drag mic button to any position on screen
- Wake word: say "Haron [command]" or "Haron" then command (enable in settings)
- Mic stops when app is minimized, resumes on return
- Panel targeting: add "top/upper/first" or "bottom/lower/second" to any command
  Example: "back top", "open downloads bottom", "copy in first"
- Folder navigation: "open [folder]", "open folder [name]" — finds folder across entire storage
  Prefixes: открой, открыть, открой папку, перейди в, зайди в / open, open folder, go to, navigate to
  Standard Russian aliases (all word forms supported):
    загрузки → Download
    документы → Documents
    картинки, изображения → Pictures
    фотографии → DCIM
    камера → Camera
    фильмы, видео → Movies
    музыка → Music
    телеграм → Telegram
    ватсап, вотсап, вацап → WhatsApp
    скриншоты → Screenshots
    блютуз → Bluetooth
    уведомления → Notifications
    рингтоны → Ringtones
    подкасты → Podcasts
    фото → DCIM
  Any other folder found by fuzzy search across entire storage (up to 3 levels deep)
- Open dialog/menu/shelf auto-closes when new voice command is given
- All commands and phrase variants:
  - Menu: меню, открой меню / menu, open menu
  - Shelf: полка, полку, буфер / shelf, clipboard
  - Hidden: скрытые, показать скрытые, спрятанные / hidden, show hidden
  - Create: создать, создай, новый, новая / create, new
  - Search: поиск, найти, найди / search, find
  - Terminal: терминал, консоль, командная строка / terminal, console
  - Select all: выделить все, выдели все / select all
  - Refresh: обновить, обнови / refresh, reload
  - Home: домой, на главную, корень / home, root, go home
  - Sort: сортировка [имя/размер/дата/тип] [вверх/вниз] / sort [name/size/date/type] [asc/desc]
  - Settings: настройки, параметры / settings
  - Transfer: передача, передать, отправить / transfer, send
  - Trash: корзина, мусор / trash
  - Storage: анализ, память, хранилище / storage, analysis
  - Duplicates: дубликаты, копии / duplicates
  - Apps: приложения, апк / apps, apk
  - Scanner: сканер, скан, штрихкод, qr / scanner, scan, barcode, qr
  - Logs: логи / logs
  - Logs pause: логи стоп, логи пауза / logs stop, logs pause
  - Logs resume: логи старт, логи продолжить / logs start, logs resume
  - Back: назад / back, go back
  - Forward: вперёд / forward, go forward
  - Up: наверх, родительская / up, go up
  - Delete: удалить, удали / delete, remove
  - Copy: копировать, копируй, скопируй / copy
  - Move: переместить, перемести, перенести, перенеси / move
  - Rename: переименуй [имя], переименовать в [имя] / rename [name], rename to [name]
  - Archive: архивировать, заархивируй, запаковать, запакуй / archive, zip
  - Extract: распаковать, распакуй, извлечь, разархивируй / extract, unpack, unzip
  - Properties: свойства, информация / properties, info
  - Deselect: снять выделение, сними выделение / deselect
  - Go to: открой [папку], перейди в [папку], зайди в [папку] / open [folder], go to [folder]
  - Refresh cache: обнови кэш, обнови голосовой кэш / refresh cache
  - Secure folder: защищённые, защита, сейф / secure, protected

# Terminal
- Shell command execution
- Color output (16/256/RGB)
- Tab path autocomplete
- Clickable paths in output
- Command history (200)
- SSH connection to remote servers (ssh user@host [-p port])
- Interactive shell via SSH (cd, env, aliases persist)
- Password dialog with save option (AES-256 encryption)
- SSH control panel: Enter, Tab, Ctrl+C/D/Z/L, Esc, arrows, Home/End
- SSH badge and disconnect button in header

# Default Opener
- Registered as system file handler
- Open from any app via "Open with"
- Receive files via Share

# File Comparison
- Text diff: two columns with sync scroll
- Color coding: green/red/yellow
- Folder comparison
- Auto mode detection

# Cloud Storage [NEW]
- Google Drive, Dropbox, Yandex Disk [NEW]
- Secure OAuth2 authentication with PKCE [NEW]
- Browse cloud files in dual-panel mode (just like local files) [NEW]
- Clickable breadcrumbs for cloud folder navigation [NEW]
- Folder item count displayed for cloud directories [NEW]
- Quick Preview for cloud images with high-res thumbnails [NEW]
- Cloud gallery: fullscreen viewing with swipe between photos [NEW]
- Video and audio streaming from cloud (plays instantly, no full download) [NEW]
- Open and edit text files from cloud [NEW]
- Save edited files back to cloud, to local storage, or both [NEW]
- Download cloud files to local storage [NEW]
- Upload local files to cloud (files up to 500MB+) [NEW]
- Reliable upload: 8-10MB chunks with automatic retry on network failure [NEW]
- Rename files and folders in cloud [NEW]
- Delete files from cloud (including from Quick Preview) [NEW]
- Create folders in cloud [NEW]
- Drag and drop files between local storage and cloud [NEW]
- Parallel transfers with individual cancel for each [NEW]

# Appearance
- Theme: system / light / dark
- Material Design 3
